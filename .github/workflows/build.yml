name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Cordova
        run: npm install -g cordova

      - name: Create Cordova Project
        run: |
          cordova create fiora com.fiora.app fiora
          cp index.html fiora/www/index.html
          cp "فيورا-final.js" "fiora/www/فيورا-final.js"
          cp logo_512x512.png fiora/www/logo_512x512.png
          cp config.xml fiora/config.xml

      - name: Add Android Platform
        working-directory: fiora
        run: cordova platform add android

      - name: Build APK
        working-directory: fiora
        run: cordova build android

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: fiora-apk
          path: fiora/platforms/android/app/build/outputs/apk/debug/app-debug.apk
